# Пока переменная не задана, приложение хранит данные в браузере (localStorage).
# Когда появится сервер с базой — впишите его адрес, и те же экраны начнут
# работать с ним. Контракт эндпоинтов описан в src/data/http.ts.
# VITE_API_URL=https://api.ikrima.example
